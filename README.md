# Seeded Grid Tool

A React/TypeScript tool for generating deterministic random grids from `40x40` and `80x80` cells.

## Scripts

- `npm run dev` starts the Vite dev server.
- `npm run build` type-checks and builds the app.
- `npm test` runs the Vitest suite.

The generation rules live in `docs/grid-rulebook.md`.