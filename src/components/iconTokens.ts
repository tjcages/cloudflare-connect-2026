export const ICON_STROKE_WIDTH = 1.7;
export const ACTION_ICON_SIZE = 14;
/** Icons in components/layers lists, picker, and drag ghost — keep in sync with `App.css` `--component-list-preview-size`. */
export const SIDEBAR_LIST_ICON_PX = 16;
/** Rect marker list glyph: slightly smaller so the filled square reads less heavy next to line icons. */
export const RECT_MARKER_LIST_GLYPH_PX = Math.round(SIDEBAR_LIST_ICON_PX * 0.8);
/** Left rail tab glyphs only (narrower chrome). */
export const RAIL_TAB_ICON_PX = 14;
/** Rail tab stroked icons: +1 vs default `strokeWidth` in `iconRegistry` (e.g. 1.25 → 2.25). */
export const RAIL_TAB_ICON_STROKE_WIDTH = 2.25;
