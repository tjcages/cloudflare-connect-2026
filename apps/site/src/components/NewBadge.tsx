export default function NewBadge() {
  return (
    <span className="relative flex h-16 shrink-0 items-center justify-center rounded-full px-8 font-mono text-[10px] leading-12 font-semibold tracking-[0.2px] text-orange-900 uppercase before:inside-border before:border-border-default">
      New
    </span>
  );
}
