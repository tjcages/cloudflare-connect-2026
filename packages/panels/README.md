# @tjcages/panels

Vendored local pre-release build of [`tjcages/shader-panel`](https://github.com/tjcages/shader-panel), built from the `ty/off-455-adopt-the-connect-dev-panel-look` branch at commit `00e2ac9` (shader-panel PR #4). Replace with the published package once `@tjcages/panels` is released.

The Connect hero imports the explicit `@tjcages/panels/dev` entry so its tuning panel remains available in production-mode branch previews. Replace this vendor directory with the published package once `@tjcages/panels` is released.
